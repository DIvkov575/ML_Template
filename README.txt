# TMP

1) fork repo
2) press green "code" button -> codespace
  2a) If you already have one - use it
  2b) Else Create new

If you create a new codespace: run "make initialize" by copying into terminal and pressing enter
else: run "make setup"


